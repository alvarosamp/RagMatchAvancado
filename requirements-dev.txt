# =============================================================================
# Dependências de DESENVOLVIMENTO (não vão para produção)
#
# CONCEITO MLOPS: separar ambientes
#   requirements.txt      → imagem Docker de produção (mínimo necessário)
#   requirements-dev.txt  → máquina do desenvolvedor + CI/CD (com ferramentas)
#
# Instalar:
#   pip install -r requirements-dev.txt
#
# No CI/CD (GitHub Actions), também usamos este arquivo.
# Em produção (Docker), só requirements.txt é copiado para a imagem.
# =============================================================================

# ── Produção (herda todas as deps do backend) ─────────────────────────────────
-r backend/requirements.txt

# ── Testes ────────────────────────────────────────────────────────────────────
pytest>=8.0               # framework de testes
pytest-cov>=5.0           # cobertura de código (--cov)
pytest-asyncio>=0.23      # suporte a testes async (FastAPI)
httpx>=0.27               # TestClient do FastAPI (substitui requests nos testes)

# ── Linting e Formatação ──────────────────────────────────────────────────────
ruff>=0.4                 # linter ultra-rápido (substitui flake8 + isort + pyupgrade)
black>=24.0               # formatador de código

# ── Utilitários de Teste ──────────────────────────────────────────────────────
faker>=25.0               # geração de dados falsos para fixtures
respx>=0.21               # mock de chamadas HTTP (útil para mock do Ollama)

# ── YAML (scripts MLOps leem mlops/configs/experiment.yaml) ──────────────────
pyyaml>=6.0

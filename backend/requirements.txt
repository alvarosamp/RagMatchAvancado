# API
fastapi>=0.111.0
uvicorn[standard]>=0.29.0
python-multipart>=0.0.9       # upload de arquivos

# Banco
sqlalchemy>=2.0.0
psycopg2-binary>=2.9.9
pydantic-settings>=2.0.0
pgvector>=0.3.0               # integração pgvector + SQLAlchemy

# OCR / Parser
docling>=2.0.0                # pip install docling

# LLM / Embeddings (Ollama local)
ollama>=0.2.0                 # pip install ollama
# modelos necessários:
#   ollama pull nomic-embed-text   (embeddings 768d)
#   ollama pull llama3             (matching LLM)

# Utilitários
pydantic>=2.0.0
python-dotenv>=1.0.0
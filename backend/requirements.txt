# ── API ───────────────────────────────────────────────────────────────────────
fastapi
uvicorn
python-multipart

# ── Banco ─────────────────────────────────────────────────────────────────────
sqlalchemy
psycopg2-binary
pydantic-settings
pgvector

# ── OCR / Parser ──────────────────────────────────────────────────────────────
docling

# ── LLM / Embeddings ──────────────────────────────────────────────────────────
ollama

# ── Exportação ────────────────────────────────────────────────────────────────
openpyxl
reportlab

# ── Utils ─────────────────────────────────────────────────────────────────────
pydantic
python-dotenv

# =============================================================================
# MLOPS — novas dependências
# =============================================================================

# MLflow: experiment tracking, model registry, UI de comparação de runs
mlflow>=2.12.1

# Prefect: orquestração de pipelines — descomente na etapa do Job Orchestrator
# prefect>=2.19.0

# Evidently: monitoramento de drift em dados e modelos
evidently>=0.4.22

# Pandas: necessário para o Evidently processar DataFrames
pandas>=2.0.0